# Core
anthropic>=0.40.0
httpx>=0.27.0
playwright>=1.44.0

# Vector store + embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Dev / testing
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: for running n8n locally via Docker — see README
# docker (system package, not pip)
